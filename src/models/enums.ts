export enum FilterType {
  COLOR = "COLOR",
  TYPE = "TYPE",
  GENDER = "GENDER",
  PRICE = "PRICE",
}

export enum ColorType {
  RED = "red",
  GREEN = "green",
  BLUE = "blue",
}

export enum GenderType {
  MEN = "MEN",
  WOMEN = "WOMEN",
}
